{
  "db": {
    "host": "${MONGO_HOST}",
    "port": "${MONGO_PORT}",
    "url": "",
    "database": "${MONGO_DATABASE}",
    "password": "${MONGO_PASSWORD}",
    "name": "db",
    "user": "${MONGO_USER}",
    "connector": "mongodb"
  }
}
